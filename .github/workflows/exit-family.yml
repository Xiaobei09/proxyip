name: Exit family check

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Update proxy list"]
    types: [completed]

permissions:
  contents: write

concurrency:
  group: exit-family
  cancel-in-progress: false

jobs:
  check:
    name: Actual exit IPv4/IPv6 family
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Skip if superseded or no data change
        id: gate
        run: |
          # 1. 竞态检查
          LATEST=$(gh run list --workflow="${{ github.workflow }}" \
            --json=databaseId,status \
            --jq '[.[] | select(.status == "in_progress" or .status == "queued")]
                   | sort_by(.databaseId) | last | .databaseId // empty' 2>/dev/null || echo "")
          if [ -n "$LATEST" ] && [ "$LATEST" != "${{ github.run_id }}" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "::notice::Superseded by run $LATEST, skipping"
            exit 0
          fi
          # 2. 触发者成功检查
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            CONCLUSION="${{ github.event.workflow_run.conclusion }}"
            if [ "$CONCLUSION" != "success" ]; then
              echo "skip=true" >> "$GITHUB_OUTPUT"
              echo "::notice::Triggering run concluded: $CONCLUSION, skipping"
              exit 0
            fi
          fi
          # 3. 数据变更检查：上游有新提交但本工作流尚未处理则运行
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            LAST_UPSTREAM=$(git log --oneline -1 --grep="update proxy list" --format=%H 2>/dev/null || echo "")
            LAST_DOWNSTREAM=$(git log --oneline -1 --grep="exit ipv4/ipv6 split" --format=%H 2>/dev/null || echo "")
            if [ -n "$LAST_UPSTREAM" ] && [ -n "$LAST_DOWNSTREAM" ]; then
              if git merge-base --is-ancestor "$LAST_UPSTREAM" "$LAST_DOWNSTREAM" 2>/dev/null; then
                echo "skip=true" >> "$GITHUB_OUTPUT"
                echo "::notice::No new upstream data since last exit family check, skipping"
              fi
            fi
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Set up Python
        if: steps.gate.outputs.skip != 'true'
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Run tests
        if: steps.gate.outputs.skip != 'true'
        run: python -m unittest discover -s tests -v

      - name: Exit family check
        if: steps.gate.outputs.skip != 'true'
        run: python scripts/exit_family.py

      - name: Annotate and classify
        if: steps.gate.outputs.skip != 'true'
        run: python scripts/annotate_classify.py

      - name: Show summary
        if: steps.gate.outputs.skip != 'true'
        run: |
          echo "IPv4 exit:"
          wc -l data/valid/all_ipv4.txt 2>/dev/null || true
          echo "IPv6 exit:"
          wc -l data/valid/all_ipv6.txt 2>/dev/null || true
          echo "Family distribution:"
          jq -r '[.proxies[].family] | group_by(.) | map({(.[0]): length}) | add // empty' \
            data/quality/exit_family.json 2>/dev/null || true

      - name: Commit changes
        if: steps.gate.outputs.skip != 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add -Af data/ ':(exclude)data/raw' ':(exclude)data/download' \
            ':(exclude)data/output' ':(exclude)data/diff'
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "chore(data): exit ipv4/ipv6 split + suffix filling ($(date -u +%Y-%m-%dT%H:%M:%SZ))"
          for i in 1 2 3 4 5; do
            if git push; then
              echo "Pushed successfully"
              exit 0
            fi
            echo "Push attempt $i failed, reapplying local data on origin/main..."
            git fetch origin main || { sleep 5; continue; }
            git reset --mixed origin/main
            git add -Af data/ ':(exclude)data/raw' ':(exclude)data/download' \
              ':(exclude)data/output' ':(exclude)data/diff'
            if git diff --cached --quiet; then
              echo "No changes to push"
              exit 0
            fi
            git commit -m "chore(data): exit ipv4/ipv6 split + suffix filling ($(date -u +%Y-%m-%dT%H:%M:%SZ))"
          done
          echo "Push failed after 5 attempts"
          exit 1
